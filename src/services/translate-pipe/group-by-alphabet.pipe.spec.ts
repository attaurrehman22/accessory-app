import { GroupByAlphabetPipe } from './group-by-alphabet.pipe';

describe('GroupByAlphabetPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByAlphabetPipe();
    expect(pipe).toBeTruthy();
  });
});
