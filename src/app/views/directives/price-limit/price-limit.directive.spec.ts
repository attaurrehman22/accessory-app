import { PriceLimitDirective } from './price-limit.directive';

describe('PriceLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceLimitDirective();
    expect(directive).toBeTruthy();
  });
});
